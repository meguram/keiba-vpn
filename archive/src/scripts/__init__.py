"""Operational Python CLI scripts.

This package is intentionally a thin namespace for runnable CLI scripts. The
modules live in sub-packages by role (``scraping``, ``data``, ``docs``,
``maintenance``). Shell-script counterparts (``server``, ``cron``) remain in
the top-level ``scripts/`` directory.
"""
