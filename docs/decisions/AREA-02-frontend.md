# AREA-02 — フロントエンド要件
**Status**: REVISED | **Last Updated**: 2026-07-08 | **Consolidates**: 旧 AREA-05 (廃止), keiba-vpn-git 既存実装からの UI 継承

---

## 0. 改訂方針

本ドキュメントは `keiba-vpn-git` 既存実装のUIを基準として、新アーキテクチャ（AREA-01 仕様）向けに再定義する。
**継承対象**: デザインシステム・ページ構成・ユーザー向け情報提示・インタラクションパターン
**非継承対象**: DB スキーマ・スクレイピング実装・ML パイプライン（AREA-01/03/06/07 で管理）
管理者専用ツール（スクレイプ操作・キュー監視・ログ閲覧）はユーザー機能ではないため本仕様に含めない。

---

## 1. デザインシステム

### 1-1. カラーパレット（ダーク Navy テーマ）

```css
/* ベース */
--bg:        #0a0e17;  /* ページ背景 */
--surface:   #131926;  /* カード・パネル背景 */
--surface2:  #1a2235;  /* ネストされた要素背景 */
--border:    #243049;  /* ボーダー全般 */

/* テキスト */
--text:      #c8d6e5;  /* 本文 */
--text-dim:  #6b7d95;  /* 補足・ラベル */

/* アクセント */
--accent:    #3b82f6;  /* 主要アクション・アクティブ状態 */
--ok:        #22c55e;  /* 成功・良好 */
--warn:      #f59e0b;  /* 警告 */
--err:       #ef4444;  /* エラー・危険 */
--purple:    #a78bfa;  /* 副アクセント（血統機能） */
--cyan:      #06b6d4;  /* データ分析系 */
--teal:      #2dd4bf;  /* トラックスピード系 */
```

> ページ固有の例外:
> - 血統ベクトル空間・血統マップ: `--accent: #ff6b35` (orange), `--accent2: #00d4aa` (teal)
> - Myostatin ページ: GitHub exact palette (`--bg: #0d1117`, `--accent: #58a6ff`)
> - レース品質分析: teal系アクセント + 9系列グラフカラー (`--c0`〜`--c8`)

### 1-2. フォント

| 用途 | フォント |
|---|---|
| 本文・UI全般 | Inter |
| 日本語テキスト | Noto Sans JP |
| 血統ページの見出し | Shippori Mincho（セリフ） |
| 数値・コード・ID | JetBrains Mono / SFMono-Regular |

### 1-3. 枠番カラーシステム（全ページ共通）

馬番の枠色は全ページで統一する。

| 枠番 | 色 | 備考 |
|---|---|---|
| 1 | 白 (`#ffffff`) + 黒枠 | |
| 2 | 黒 (`#1a1a1a`) | 白文字 |
| 3 | 赤 (`#ef4444`) | 白文字 |
| 4 | 青 (`#3b82f6`) | 白文字 |
| 5 | 黄 (`#f59e0b`) | 黒文字 |
| 6 | 緑 (`#22c55e`) | 白文字 |
| 7 | オレンジ (`#f97316`) | 白文字 |
| 8 | ピンク (`#ec4899`) | 白文字 |

### 1-4. 共通コンポーネントパターン

| コンポーネント | 仕様 |
|---|---|
| カード | `background: --surface`, `border: 1px solid --border`, `border-radius: 10-12px`, `padding: 16px` |
| ステータスバッジ | インライン pill、状態別 bg+color（ok/warn/err/accent） |
| テーブル | `border-collapse: collapse`, sticky thead、`tr:hover` ハイライト |
| ボタン | `.btn`（accent bg、角丸）、`.btn-outline`（透明 + border）、`.btn-sm`、`.btn-secondary` |
| プログレスバー | 細い水平 fill bar in `.bar-bg` コンテナ |
| スピナー | CSS `border-top-color` + `@keyframes spin` |
| トースト通知 | 右下固定、4秒で自動消去 |
| ツールチップ | CSS `::after + content:attr(data-tip)` またはカスタム floating div |

### 1-5. レイアウト原則

- 全ページで `--bg` ダーク背景
- ナビゲーション高さ: 44px 固定（sticky top）
- モバイル対応: ナビをブランド下に全幅ストリップとして折りたたむ
- 右下透かしロゴ: 40×40px、opacity 0.12、pulse アニメーション

---

## 2. ナビゲーション構造

### 2-1. グローバルナビ（全ページ共通）

Sticky top bar（44px）。5 カテゴリのドロップダウングループ。

| カテゴリ | 色 | 主要ページ |
|---|---|---|
| Home | — | ダッシュボード (`/`) |
| AI予測 | `#58a6ff`（青） | レース詳細、レース品質分析、位置追跡難易度、今週の血統傾向分析 |
| 血統 | `#bc8cff`（紫） | 血統分析、血統ベクトル、血統適性マップ、種牡馬メモ |
| データ分析 | `#39d2c0`（シアン） | 詳細データ分析、成長曲線、トラックスピード指数、AI SLA |
| 馬券最適化 | `#3fb950`（緑） | Kelly 基準 馬券最適化 |

ブランドロゴ: SVG 馬頭ロゴ + conic-gradient ring アニメーション

### 2-2. ページ内ナビ

- レース詳細: 4タブ（出馬表・レース結果・AI予測・出走馬詳細）
- データ分析ページ: ページ内サブタブ（種別ごと）
- 血統分析: 2タブグループ（距離適性・コース特性）

---

## 3. ユーザー種別とアクセス制御

| 機能 | ゲスト | ログイン済 |
|---|---|---|
| レース一覧・基本情報 | ○ | ○ |
| AI予測（全頭） | 上位3頭のみ | ○ |
| 出走馬過去成績（10走） | ✗ | ○ |
| データ分析全機能 | ○ | ○ |
| マイ分析（条件保存） | ✗ | ○ |
| 馬券最適化 | ✗ | ○ |
| 血統ツール全機能 | ○ | ○ |
| お気に入り馬登録・一覧（F-12） | ✗ | ○ |
| レース出走メール通知（F-09） | ✗ | ○ |

認証: パスワードのみ（ユーザー名なし）、「30日間ログイン保持」チェックボックス付きログイン画面。JWT を HttpOnly Cookie に保存、CSRF 対策付き（SameSite=Strict）。`Authorization: Bearer` ヘッダーは使用しない。

---

## 4. ページカタログ

### 4-1. ダッシュボード (`/`)

**目的**: 中央ハブ。最新開催日・レース一覧へのクイックアクセス。

**表示要素:**
- 5カテゴリのカードグリッド（各カテゴリ色でハイライト）
- サーバー接続ステータスチップ（green=OK / red=ERROR）
- 最新8開催日チップ（クリックで当日レース一覧へ）
- 本日または直近のレース一覧パネル

---

### 4-2. レース一覧

**目的**: 指定日の全レースカード表示。

**フィルタ**: 開催場（東京/阪神/中京 等）

**レースカード表示項目**:
- 開催場・R（ラウンド）番号、開始時刻
- グレードバッジ（G1=赤、G2=青、G3=緑）
- レース名
- 芝/ダート・距離・馬場状態・頭数
- 「AI予測 あり」インジケータ（予測データ存在時）

---

### 4-3. レース詳細 (`/race/{race_id}`)

**目的**: 単一レースの全情報ハブ。**4タブ構成**。

#### レースヘッダー（全タブ共通）
- ラウンドバッジ、グレードバッジ
- レース名
- 日時、開催場、芝/ダート・距離・回り、天候・馬場状態、発走時刻
- netkeiba 外部リンクボタン

---

#### タブ1: 出馬表 (shutuba)

ソート可能テーブル。

| カラム | 説明 |
|---|---|
| 枠番 | 枠番カラーシステムの丸 |
| 馬番 | |
| 馬名 | |
| 性齢 | |
| 斤量 | |
| 騎手 | |
| 調教師 | |
| 単勝オッズ | `<5.0`=赤、`<20`=橙、それ以上=暗色 |

---

#### タブ2: レース結果

**(a) ペース・ラップパネル**
- キーパスタイム: 1F・前3F・後3F・上り3F タイムカード
- ラップストリップ: 各ハロンをカラーセル（high=赤/黄、mid=灰、slow=青）+ 上に SVG 折れ線グラフオーバーレイ
- ペースパターンバッジ: slow_burst（青）/ consuming（赤）/ sustained（緑）/ standard（灰）

**(b) コーナー通過順位パネル**
- 1〜4角ごとにグループ化、位置差をスペーシングで表現
- 馬ピル: 枠番カラー色付き丸バッジ。クリックで対応着順行へスクロール。

**(c) 着順テーブル**

| カラム | 説明 |
|---|---|
| 着順 | 1=金、2=銀、3=銅 ハイライト |
| 枠・馬番 | 枠番カラー |
| 馬名 | |
| 性齢・斤量・騎手 | |
| タイム・着差 | |
| 通過順 | |
| 上り3F | |
| 人気・単勝 | |

**(d) 払戻金セクション**
- 馬券種別カードグリッド: 単勝・複勝・枠連・馬連・ワイド・馬単・3連複・3連単
- 各カード: 組み合わせ（枠番カラーの数字ピル）＋ 払戻額

---

#### タブ3: AI予測

> F-13/F-14 要件はここに統合。ゲストは上位3頭のみ表示。

グループヘッダー付きカラム構成:

| グループ | カラム |
|---|---|
| 出走馬 | 馬番、馬名 |
| 予測確率（T-1〜T-3） | 勝率%、連対率%、複勝率% |
| 想定オッズ（T-4〜T-5） | 単勝(倍)、複勝↓(倍)、複勝↑(倍) |
| 期待値（T-6〜T-7） | 単回収率、複回収率 |
| 位置・脚質（T-8〜T-9） | 予測着順、脚質予測 |
| 印 | ◎本命・○対抗・▲単穴・★推奨 |

- 回収率 ≥ 100: 緑太字 + 行ハイライト（バリューベット、F-14）
- 1着予測行 = 金背景、2着 = 銀背景、3着 = 銅背景
- ペースカテゴリ（T-10）: HIGH=赤バッジ / MIDDLE=青バッジ / SLOW=緑バッジ
- ラップ予測グラフ（T-11、F-15）: 折れ線（X=ハロン番号、Y=ラップ秒）、Chart.js

**ゲスト制限バナー**: 上位3頭表示の旨＋ログインリンク。

**注釈**: 「※ この統計はリアルタイム集計です。AIモデルが予測に使用した時点の特徴量とは異なる場合があります。」

---

#### タブ4: 出走馬詳細

- 左サイドバー: 馬ピッカーリスト（枠番カラー丸 + 馬名）
- 右パネル:
  - プロフィール DL: 性別・生年月日・調教師・馬主・生産者・父・母・母父・獲得賞金
  - 近10走テーブル: 日付・場所・レース名・着順・距離・タイム・馬場・騎手

---

### 4-4. AI予測: 位置追跡難易度 (`/tracking-difficulty`)

**目的**: 出走馬ごとの位置取りのしやすさ（ease スコア）を予測・可視化。

**コントロール**: 日付選択、開催場選択、レース選択、分析実行ボタン

**ローディング UI**:
- ステップインジケーター（準備→取得→計算→表示）付きスピナーカード
- shimmer スケルトンカード
- 経過タイマー

**表示パネル一覧**:

**(a) Gate Ease テーブル** — ソート可能

| カラム | 説明 |
|---|---|
| 枠・馬番・馬名 | 枠番カラー |
| 脚質 | 逃げ/先行/差し/追込 バッジ |
| 想定ポジション | |
| 想定展開フロー | |
| 想定上り3F | |
| 容易度バー + ラベル | 非常に楽=緑 / 楽=青 / 普通=灰 / やや困難=橙 / 困難=赤 |

**(b) マスター馬テーブル**（ソート4モード）

| カラム | 説明 |
|---|---|
| 枠・馬番・馬名 | |
| 脚質 | バッジ |
| 容易度 | バー + ラベル |
| 温存 | スタミナ指数 |
| 序→中→終 | ポジションフロー |
| 上り3F予測 | |
| 展開パターン | フローバッジ |
| 最前位置% | |
| コース | 距離変化 + 馬場一致 |

**(c) 前走位置取り（5段階）分布**
- ポジション分布 bins に枠番カラーの馬ピル

**(d) ペース予想パネル**
- ペースバッジ（ハイ=赤 / ミドル=青 / スロー=緑）
- ペースメーターバー
- テキスト叙述コメント
- 判定 + 馬券ヒント

**(e) 序盤ポジション(1-3F)ラダー**
- 縦軸=ポジション帯（1-3位、4-6位、7-9位 等）、各バンドに枠番カラーの馬ピル

馬名クリック: モーダルで近10走履歴テーブル表示。

---

### 4-5. AI予測: 成長曲線 (`/growth-curve`)

**目的**: 1頭の馬のキャリアアーク（馬体重・タイム指数の推移）を可視化。

**コントロール**:
- 馬名オートコンプリート検索（300ms デバウンス）
- 表示件数選択（5/10/20/30/50/全て）
- 強制再読み込みボタン

**サマリーカード**: 最高着順、平均着順、総出走数、馬体重変動幅、デビュー体重、直近3戦平均、デビューとの体重差

**フィルターバー**: 馬場種別チップ（芝=緑 / ダート=橙 / 障害=紫）、距離 min/max、自動生成プリセット

**グラフ1 — 馬体重 × タイム指数**（Chart.js v4、デュアルY軸 棒+折れ線）
- 棒グラフ = 馬体重（馬場種別 × 距離で色相変化: 芝=142°/ダート=30°/障害=270°）
- 折れ線 = タイム指数（金 `#d29922`）
- 閾値ゾーン: 100（緑）、110（黄）
- 着順バッジ（オーバーレイ）

**グラフ2 — レース間隔 × タイム指数**
- 棒グラフ = 間隔週数（<2週=赤 / >13週=青 / 通常=灰）
- 折れ線 = タイム指数
- 着順バッジ

**レーステーブル**: 日付・競馬場・レース名・コース（馬場+距離）・着順（金/銀/銅）・馬体重・増減・間隔・タイム・指数

URL state: `?horse_id=X&limit=N`

---

### 4-6. データ分析: トラックスピード指数 (`/track-speed`)

**目的**: 競馬場・馬場ごとの速度レベルを Z スコア正規化指数（TSI）で表示。50=標準、高=速い。

**コントロール**: 日付選択、開催場選択、表示ボタン

**スピードレベル凡例（5段階）**:
| レベル | 名称 | 色 |
|---|---|---|
| 5 | 超高速 | `#ef4444`（赤） |
| 4 | 高速 | `#f97316`（橙） |
| 3 | 標準 | `#a3a3a3`（灰） |
| 2 | 低速 | `#3b82f6`（青） |
| 1 | 超低速 | `#8b5cf6`（紫） |

**表示セクション**:
- **日付別タイムライン**（折りたたみ可能）: 日付ラベル・曜日（土=青/日=赤）・開催場バッジ（TSI値 + スピードレベルバッジ + サンプル数）
- **開催場詳細パネル**（展開可能）: レース別テーブル（R・距離・グレード・タイム・基準タイム・Z値・ペース補正後Z・TSI）。芝行=緑左枠 / ダート行=橙左枠。
- **メインレーステーブル**: R・コース・グレード・馬場状態・レース名（netkeiba リンク）・タイム・基準・Z値・ペース補正・TSI指数・PF指数・PF(2000m)。ソート可・行クリックで馬別サブテーブル展開（着順: 1着=金/2着=accent色）。
- **コースカテゴリ検索**: 開催場+馬場+スピードカテゴリ選択、結果テーブル（ページネーション付き）
- **分布チャートセクション**: 開催場別 SVG スパークチャート（TSI 経時変化）

---

### 4-7. データ分析: レース品質分析 (`/race-quality`)

**目的**: NNLS（非負最小二乗法）による8アーキタイプのレース分類。

**コントロール**: 日付選択・開催場フィルタ・レース選択・手動日付入力・開催日まとめ読み込みボタン・単レース分析ボタン

**表示モード**:
- **日単位ビュー**: 水平スタック棒グラフ（√頭数 加重平均、1棒=1開催日）
- **セグメントビュー**: 馬場・距離セグメント別スタック棒グラフ

**レーステーブルカラム**:
- 条件帯・場所・距離/馬場/コース
- 消/溜/均 ペース形状（0-100）
- レース名（`/race/{id}` リンク）・頭数・R²
- 上位傾向（上位3アーキタイプラベル + %）
- 分布（ミニスタック棒グラフ）

**単レースビュー**: アーキタイプ別確率バー行（% + R² + ペース形状）

**適性マトリクスパネル**: 馬 × 8軸 ヒートマップ（レース選択時自動表示）

---

### 4-8. 血統: 適性マップ (`/note-aptitude-race`)

**目的**: 特定レースの馬ごとの血統適性を SVG パン/ズームマップで可視化。

**3パネルレイアウト**（フルビューポート）:
- **左サイドバー (260px)**: 日付→開催場グループのレースリスト。コース情報カード（馬場ピル・距離ピル・状態ピル）。
- **中央 SVG マップ** (`viewBox="-8 -6 16 12"`、ホイールズーム・ドラッグパン):
  - 背景グリッドライン
  - クラスター楕円（緑=適合良 / 赤=適合不良）
  - ベストゾーン楕円（青破線）
  - 同日傾向楕円（橙破線）
  - 馬ドット（枠番カラー丸、上位3頭=金/銀/銅リング）
  - 軸ラベル
- **右パネル (290px)**: `course_match` スコア順に馬カード
  - ランクバッジ・枠番丸・馬名・L2クラスターラベル（色付き）
  - 父・母父名
  - コース適性バー + スコア
  - 状態リフトタグ（緑=プラス / 赤=マイナス）
  - 上位3頭サマリーバー

**ホバーツールチップ**: 馬名・L2クラスター・父・母父・母父系統・コース適合スコア・状態リフト

---

### ~~4-9. 血統: クラスター検索 (`/bloodline-cluster`)~~ — **廃止済み（2026-07-08）**

> このページは UI として不要と判断し削除された。`frontend/app/bloodline-cluster/` ディレクトリ・ページファイルは削除済み。ホームダッシュボードおよび `/bloodline` ナビからも除去済み。以下の仕様は参考記録として保持。

**目的**: 馬または種牡馬の血統クラスター分類と適性プロファイルを検索表示。

**コントロール**: オートコンプリート検索入力（馬名サジェスト）、例チップ

**検索結果 — 適性プロファイルカード**:
- 馬名 + メタ（性別・年齢）
- 上位 L2 クラスター行（水平バー + スコア）
- 2カラムグリッド: 血統テーブル（位置・世代・L2ラベル）| 成績統計（勝利数・複勝・主要条件）
- 統計バッジ: 総出走・勝率・複勝率・最良距離/馬場
- 注目パターンセクション: 期待値から大きくズレる条件のグリッド
- レース記録詳細（折りたたみ可）: 条件グループ別テーブル（馬場×距離×クラス）、複勝率・サンプル数・P値マーカー

---

### 4-10. 血統: 血統ベクトル空間 (`/bloodline-vector`)

**目的**: L2 血統サブグループの 2D ベクトル空間マップ（Canvas 描画）。フルビューポート3パネル。

**左パネル (280px)**:
- 配置基底ボタン（PCA / UMAP / t-SNE）
- 色分けモード: スーパーグループ / 主流×非主流 / 距離支配 / 馬場支配 / 道悪適性
- 類似度モード: 適性類似 / 血統構造類似 / ブレンド（推奨）
- PCA ローディング情報ボックス
- 凡例
- 馬/グループ検索入力（インメモリ）

**Canvas（HTML5 Canvas、バニラ JS）**:
- ノード = L2 サブグループ、radius = `8 + sqrt(n_members) * 2.2`（クランプ 10-36）
- ホバーツールチップ: グループ名・スーパーグループ・距離支配・馬場支配・道悪ラベル・メンバー数・上位馬名
- クリック → 選択ハイライト + 右パネルに類似グループ表示（コサイン類似度スコア）
- ドラッグパン・ホイールズーム・+/-ボタン

**カラーパレット（5モード×最大5色）**:
- スーパーグループ: 青/紫/橙/緑/赤
- 距離支配: 赤(短距離) / 橙(マイル) / 緑(中距離) / 青(長距離)
- 馬場支配: 緑(芝) / 橙(ダート)
- 道悪適性: 紫(巧者) / 灰(中立) / 橙(苦手)

---

### ~~4-11. 血統: 血統マップ (`/pedigree-map`)~~ — **廃止済み（2026-07-08）**

> このページは UI として不要と判断し削除された。`frontend/app/pedigree-map/` ディレクトリ・ページファイルは削除済み。ホームダッシュボードおよび `/bloodline` ナビからも除去済み。DEC-018 で決定した D3.js フォースグラフ実装は未着手のまま廃止となった。以下の仕様は参考記録として保持。

**目的**: 種牡馬系統全体の D3.js ツリー/フォースグラフ。フルビューポート3パネル。

**系統カラー（固定）**:
| 系統 | 色 |
|---|---|
| SS（サンデーサイレンス） | `#e63946` |
| KK（キングカメハメハ） | `#457b9d` |
| RB（ロベルト） | `#2a9d8f` |
| ND（ノーザンダンサー） | `#e9c46a` |
| DH（ダンシングブレーヴ） | `#f4a261` |

**表示モード**:
- 探索モード（2カラム: 320px フォーム + キャンバス）:
  - 検索フォーム: 期間・開催場・馬場・距離・馬場状態・クラス/グレード・着順・人気・オッズ・血統世代
  - 結果テーブル: 種牡馬統計（勝率/複勝率/ROI タブ切り替え）
- 研究モード: 異なる3パネルレイアウト

**右パネル（ノードクリック時）**: 種牡馬名（Shippori Mincho フォント）・系統タグチップ・統計グリッド（総出走・勝率・複勝率・平均距離）・類似種牡馬リスト・近親交配クロス・適性タグチップ

---

### 4-12. AI予測: 今週の血統傾向分析 (`/pedigree-race-stats`) — **カテゴリ変更（2026-07-08）**

> 旧: 「血統: 種牡馬成績クエリ」。ホームダッシュボードのカテゴリを「血統」→「AI予測」配下に移動し、名称を「今週の血統傾向分析」に変更。ページ URL・実装は継続。新目的: 予測されたレース質と出走馬の血統を照合し血統適性スコアを点数化する。

**目的**: 予測されたレース質と出走馬の血統を照合し、各馬の血統的優位性をスコア化・可視化する。旧来の多軸フィルタ機能を拡張し、AI予測（レース質）との連携分析が主軸となる。

**フィルターパネル**（グリッドレイアウト）:
- 期間: 日付範囲 + 年クイック選択（複数選択可）
- 開催場: トグルボタン（複数選択）
- 馬場種別: 芝/ダート/障害
- 距離: min-max 数値ペア
- 馬場状態: 良/稍重/重/不良 トグル
- クラス: グレードトグル（複数選択）
- 着順: 範囲
- 人気: 範囲
- 単勝オッズ: 範囲
- 血統世代: 1-10 範囲
- 検索ボタン・リセットボタン

**結果テーブル**:
- 統計行: 総レコード数・ユニーク種牡馬数・検索時間
- タブ: 勝率系 / 複勝率系 / 回収率系
- テーブルカラム: ランク・種牡馬名 + 各カテゴリ統計列

**ROI パネル（回収率）**:
- ハイライト: 単勝 ROI 上位=青 / 複勝 ROI 上位=紫 / 両方上位=金グラデーション

---

### 4-13. 血統（開発者向け）: Myostatin 遺伝子 (`/myostatin`) — **表示制御変更（2026-07-08）**

> ホームダッシュボードの表示制御を 🌐 → 🛠️（開発者のみ）に変更。`ADMIN_CATEGORIES` セクション「血統（開発者向け）」配下に移動。ページ自体は URL で直接アクセス可能だが、ホームナビには表示されない。

**目的**: MSTN 遺伝子型（CC/CT/TT）ごとの距離適性を追跡・可視化。

**遺伝子型カラー**:
- CC = 赤（短距離型）
- CT = 金（中距離型）
- TT = 緑（長距離型）

**表示内容**:
- 統計行: CC・CT・TT・Total カウントカード
- **散布図** (Chart.js v4): X=平均勝利距離(m)、Y=Cアレル確率、遺伝子型別カラー、信頼度別ドットサイズ（confirmed=10px, highly_likely=8px, estimated=6px, inferred=5px）
- **遺伝子型分布（ドーナツグラフ×3）**: ナレッジベース / 実測 / オープンクラス
- **育種シミュレーター**: 父・母父入力（datalist オートコンプリート）・距離入力 → CC/CT/TT 確率バー + 適合メッセージ
- **最新知見パネル**: 日付バッジ付き研究更新カード＋影響タグ
- **馬テーブル**: 馬名・英名・型バッジ・信頼度バッジ・C%・T%・平均勝距離・系統・根拠（展開可）。ソート可・検索ボックス・フィルターボタン。
- **系統別遺伝子型分布（積み上げ棒グラフ）**: 種牡馬系統別 CC/CT/TT 積み上げ

---

### 4-14. 馬券最適化 (`/betting`)

**目的**: Kelly 基準による最適馬券ポートフォリオ生成（ログイン必須）。

**入力フォーム**:
- レース ID（12桁）
- 資金（bankroll、デフォルト 100,000円）
- Kelly 倍率（デフォルト 0.25、範囲 0.05-1.0）
- 最低 EV 閾値（デフォルト 1.05）
- 馬券種別チェックボックス: 単勝・複勝・馬連・ワイド・馬単

**結果パネル**:
- 統計行: 投票総額・期待回収額（プラス=緑/中立=橙/マイナス=赤）・期待 ROI・残資金・投票数
- **投票ポートフォリオ**（左）:
  - 配分バー（馬券種別ごとのスタックセグメント）
  - テーブル: 種別バッジ・組み合わせ・オッズ・確率・EV バー（≥1.5=緑 / ≥1.1=橙 / 他=赤）・Kelly%・投票額・期待回収
- **馬ごとの勝率バー + Kelly 配分チャート**（右）

**馬券種別バッジカラー**: 単勝=赤 / 複勝=緑 / 馬連=青 / ワイド=紫 / 馬単=橙

---

### 4-15. お気に入り馬機能 (`/favorites`) — F-12

**目的**: ユーザーが馬をお気に入り登録し、一覧表示する（ログイン必須）。

**機能要件**:
- 馬名検索 / レース詳細出馬表から「お気に入り」ボタンで登録・解除
- `/favorites` 一覧ページ: 登録馬のカード一覧（馬名・性齢・直近出走日・次走予定）
- 各カードに「お気に入り解除」ボタン

**UI 要素**:
- ハートアイコンボタン（登録済=accent 塗りつぶし / 未登録=アウトライン）
- 一覧は `--surface` カードグリッド、登録馬なし時は空状態イラスト＋案内テキスト

**API 消費エンドポイント**:
- `GET  /api/v1/favorites` (auth_required: true) — 登録馬一覧取得
- `POST /api/v1/favorites` (auth_required: true) — 馬を登録
- `DELETE /api/v1/favorites/{horse_id}` (auth_required: true) — 馬の登録解除

---

### 4-16. 通知設定 (`/notifications`) — F-09

**目的**: お気に入り馬のレース出走時にメール通知を送信（SendGrid 使用）。

**機能要件**:
- 通知設定ページ: メール通知 ON/OFF トグル、通知タイミング選択（前日18時 / 当日08時）
- F-12 お気に入り馬がレース出走予定に追加されたタイミングでメール送信

**UI 要素**:
- トグルスイッチ（accent=ON / 灰=OFF）
- 送信先メールアドレス表示（変更不可）
- 最近の通知送信履歴テーブル（日時・対象馬名・レース名・送信ステータス）

**API 消費エンドポイント**:
- `GET  /api/v1/notifications/settings` (auth_required: true) — 通知設定取得
- `PUT  /api/v1/notifications/settings` (auth_required: true) — 通知設定更新

---

### 4-17. 血統: 種牡馬メモ (`/stallion-notes`) — **新規追加（2026-07-08）**

**目的**: 血統ドメイン知識ベース（274エントリ）を閲覧・編集できる内部リファレンスページ。

**アクセス制御**:
- 一般ユーザー: 種牡馬・牝系カテゴリのみ表示。編集・追加ボタン非表示。
- 開発者モード（URL に `?dev=1` を付与 → sessionStorage に保持）: 全6カテゴリ表示・編集・追加・削除が可能。ヘッダーに「DEV MODE」バッジ表示。

**カテゴリ構成**:
- 🌐: 種牡馬、牝系
- 🛠️: 各国の血統特徴、特定の海外レース、大枠分類、系統組み合わせ

**レイアウト**: ヘッダー + 左サイドバー（240px）+ 右コンテンツエリア。サイドバーはカテゴリ折りたたみ + エントリ一覧 + 検索。

**永続化**: 編集内容は localStorage（`ped_edit_{id}`）、カスタムエントリは localStorage（`ped_custom_entries`）に保存。

---

### 4-18. データ分析: 詳細データ分析 (`/data-analysis`) — **新規追加（2026-07-08）**

**目的**: Target Frontier スタイルの多次元インタラクティブデータ分析。PostgreSQL からリアルタイムに集計・可視化する。

**分析モード（4種）**:
| モード | 説明 | チャート |
|---|---|---|
| 分布分析 | カテゴリ別集計値（avg/count/sum/min/max） | 水平棒グラフ（SVG） |
| 散布図 | 2指標の相関 | 散布図（SVG、最大3,000点） |
| ランキング | 馬別集計（3戦以上）の上位N件 | 水平棒グラフ（SVG） |
| 時系列 | 月次集計トレンド | 折れ線グラフ（SVG） |

**Y軸フィールド（数値）**: 着順、タイム、上がり3F、馬体重、斤量、AI勝率予測、AI複勝率予測、期待ROI（単勝/複勝）

**X軸フィールド（カテゴリ）**: 馬場、馬場状態、グレード、競馬場、クラス、ペース（H/M/S）、距離帯（200m単位）、月、年

**グループ分け（色分け）**: X軸と同じカテゴリフィールド群から任意選択

**フィルター**: 日付範囲、馬場（芝/ダート）、距離 min/max、グレード（カンマ区切り）、最大件数

**バックエンド API**: `GET /api/v1/data-analysis/query`（フィールド名ホワイトリスト管理、最大5,000件）、`GET /api/v1/data-analysis/schema`

**レイアウト**: 左コントロールパネル（264px）+ 右メインエリア（チャート + データテーブル + クエリ詳細）

---

## 5. 技術要件

### 5-1. フレームワーク・ライブラリ

| 項目 | 採用 | 理由 |
|---|---|---|
| フレームワーク | Next.js 15（App Router） | SSR/ISR/CSR 混在、既存 Next.js 環境踏襲 |
| チャートライブラリ | Chart.js v4 | 既存実装との継続性（成長曲線・ラップ・Myostatin） |
| グラフ（D3） | D3.js v7 | 血統マップのフォースレイアウト・SVG 操作 |
| スタイリング | Tailwind CSS v3 + CSS Variables | デザインシステム変数管理 |
| 国際化 | 日本語専用（i18n 不要） |  |

### 5-2. レンダリング戦略

| ページ | 戦略 | 理由 |
|---|---|---|
| ダッシュボード (`/`) | ISR（revalidate 60s） | 開催日リストは頻繁に変わらない |
| レース一覧 | ISR（revalidate 120s） | 発走まで数日間安定 |
| レース詳細 — 出馬表タブ | ISR（revalidate 3600s） | 出馬確定後ほぼ変化なし |
| レース詳細 — 結果タブ | ISR（revalidate 300s） | 発走後30分で確定 |
| レース詳細 — AI予測タブ | CSR（SWR、TTL=発走まで） | オッズ連動でリアルタイム性必要 |
| 成長曲線・馬詳細 | CSR | ユーザー操作依存 |
| 血統ベクトル空間 | SSG（Server-injected JSON） | 事前計算データのみ |
| 血統マップ | CSR + SSG | D3 描画はクライアント |
| トラックスピード | CSR | 日付選択による動的ロード |

### 5-3. パフォーマンス目標（AREA-01 N-1/N-2 準拠）

| シナリオ | 目標 |
|---|---|
| API レスポンス（キャッシュヒット時） | ≤ 200 ms |
| API レスポンス（キャッシュミス時） | ≤ 2,000 ms |
| LCP（Largest Contentful Paint） | ≤ 2.5s（3G） |
| CLS（Cumulative Layout Shift） | ≤ 0.1 |

### 5-4. アクセシビリティ・品質目標

| 指標 | 目標 |
|---|---|
| Lighthouse Performance | ≥ 85 |
| Lighthouse Accessibility | ≥ 90 |
| モバイル対応 | 必須（ナビ折りたたみ実装） |

---

## 6. ページ階層サマリー

```
/                           ← ダッシュボード
/login                      ← ログイン（パスワード認証）

/races?date=YYYYMMDD        ← レース一覧（日付フィルタ）
/race/{race_id}             ← レース詳細（4タブ）

/tracking-difficulty        ← 位置追跡難易度分析
/race-quality               ← レース品質 NNLS 分析
/pedigree-race-stats        ← 今週の血統傾向分析（AI予測 × 血統スコア）★AI予測配下に移動
/ai-sla                     ← AI パイプライン SLA ドキュメント

/data-analysis              ← 詳細データ分析（Target Frontier スタイル）★新規追加
/growth-curve               ← 成長曲線
/track-speed                ← トラックスピード指数

/bloodline                  ← 血統 × 距離/コース研究
/bloodline-vector           ← 血統ベクトル空間
/note-aptitude-race         ← 血統適性マップ（SVG）
/stallion-notes             ← 種牡馬メモ（知識ベース）★新規追加
/myostatin                  ← Myostatin 遺伝子ダッシュボード（🛠️ 開発者のみ）

/betting                    ← 馬券最適化（Kelly 基準）

/favorites                  ← お気に入り馬一覧（F-12、ログイン必須）
/notifications              ← 通知設定（F-09、ログイン必須）

# 廃止済み（2026-07-08）
# /pedigree-map             ← 血統構造マップ（D3.js サイアー系図）→ 削除済み
# /bloodline-cluster        ← メタクラスタ判定 → 削除済み
```

---

## 7. 主要 API エンドポイント（フロントエンド消費分）

認証凡例: [public]=認証不要 (auth_required: false)、[auth]=ログイン必須 (auth_required: true)

```
# レース
GET  /api/v1/races/today                              ← 本日のレース一覧 [public]
GET  /api/v1/races?date=YYYYMMDD                      ← レース一覧 [public]
GET  /api/v1/races/{race_id}                          ← レース詳細・出馬表 [public]
GET  /api/v1/races/{race_id}/entries                  ← 出馬表 [public]
GET  /api/v1/races/{race_id}/results                  ← 着順・ラップ・コーナー [public]
GET  /api/v1/races/{race_id}/predictions              ← AI予測（T-1〜T-11）[auth]
                                                         レスポンス: win_probability: float(0.0-1.0)
                                                                     place_probability: float(0.0-1.0)

# フィルタ・集計
POST /api/v1/filter/stats                             ← フィルタ統計集計 [auth]

# 分析
GET  /api/v1/races/{race_id}/tracking-difficulty      ← 位置追跡難易度 [public]
GET  /api/v1/horse/{id}/growth-curve                  ← 成長曲線 [public]
GET  /api/v1/track-speed/day?date=X&venue=Y           ← TSI指数 [public]
GET  /api/v1/race-quality/race?id=X                   ← NNLS 分析 [public]

# データ分析
GET  /api/v1/data-analysis/query                      ← 詳細データ分析クエリ [public]
GET  /api/v1/data-analysis/schema                     ← 利用可能フィールド一覧 [public]

# 血統
GET  /api/v1/pedigree/race-note?race_id=X             ← 適性マップデータ [public]
GET  /api/v1/bloodline-cluster/lookup?q=X             ← クラスター検索 [public] ※エンドポイントは残存
GET  /api/v1/pedigree-race-stats/query                ← 今週の血統傾向分析クエリ [public]

# 馬券
POST /api/v1/betting/optimize                         ← Kelly 最適化 [auth]

# お気に入り（F-12）
GET    /api/v1/favorites                              ← 登録馬一覧 [auth]
POST   /api/v1/favorites                              ← 馬を登録 [auth]
DELETE /api/v1/favorites/{horse_id}                   ← 登録解除 [auth]

# 通知（F-09）
GET  /api/v1/notifications/settings                   ← 通知設定取得 [auth]
PUT  /api/v1/notifications/settings                   ← 通知設定更新 [auth]
```

---

## 8. 用語定義（フロントエンド関連）

| 用語 | 定義 |
|---|---|
| バリューベット | 回収率 ≥ 100 の馬 |
| TSI | Track Speed Index。Z スコア正規化馬場速度指数（50=標準） |
| PF 指数 | コース内相対評価速度指数 |
| PF(2000m) | 全コース横断比較用標準化指数（2000m 基準） |
| L2クラスター | 2段階血統クラスタリングの第2層サブグループ |
| course_match | 血統適性マップにおけるコース適合スコア |
| ease スコア | 位置追跡難易度の容易度スコア（0-100） |
| MSTN遺伝子型 | CC=短距離型 / CT=中距離型 / TT=長距離型 |
| Kelly 倍率 | Kelly 基準の分率（0.25 = Quarter Kelly） |
| win_probability | AI予測レスポンスの勝率フィールド。`float (0.0〜1.0)`（旧: `prediction_score`）|
| place_probability | AI予測レスポンスの複勝率フィールド。`float (0.0〜1.0)`（DEC-022 追加）|
