# Set to 'true' to use mock data (no backend required)
NEXT_PUBLIC_MOCK=true
NEXT_PUBLIC_API_URL=http://localhost:5000
# 監視ポータル URL（管理者のみ表示、省略時は同一ホスト:9090）
NEXT_PUBLIC_MONITOR_URL=http://localhost:9090
